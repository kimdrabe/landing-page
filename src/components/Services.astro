<style>
  .services {
    background: var(--color-bg-alt);
  }
  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-md);
    margin-top: var(--space-lg);
  }
  .service-card {
    border: 1px solid var(--color-border);
    padding: var(--space-lg) var(--space-md);
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
  }
  .service-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--color-accent);
    z-index: 0;
    transform: translateY(100%);
    transition: transform 0.4s ease;
  }
  .service-card:hover::before {
    transform: translateY(0);
  }
  .service-card > * {
    position: relative;
    z-index: 1;
  }
  .service-card:hover {
    border-color: var(--color-accent);
  }
  .service-card:hover .service-title,
  .service-card:hover .service-desc,
  .service-card:hover .service-num {
    color: var(--color-bg);
  }
  .service-card:hover .service-arrow {
    color: var(--color-bg);
    transform: translate(4px, -4px);
  }
  .service-num {
    font-family: var(--font-display);
    font-size: 3.5rem;
    line-height: 1;
    color: var(--color-accent);
    transition: color 0.4s ease;
  }
  .service-title {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 2.5vw, 2rem);
    text-transform: uppercase;
    letter-spacing: -0.01em;
    transition: color 0.4s ease;
  }
  .service-desc {
    font-size: 1rem;
    color: var(--color-text-muted);
    line-height: 1.7;
    flex: 1;
    transition: color 0.4s ease;
  }
  .service-arrow {
    font-size: 1.5rem;
    color: var(--color-accent);
    align-self: flex-end;
    transition: all 0.4s ease;
  }
  @media (max-width: 768px) {
    .services-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<section class="section services" id="services">
  <div class="container">
    <span class="section-label reveal">What We Do</span>
    <h2 class="section-title reveal reveal-delay-1">Services</h2>
    <div class="services-grid">
      <div class="service-card reveal reveal-delay-1">
        <span class="service-num">01</span>
        <h3 class="service-title">Web Design</h3>
        <p class="service-desc">Custom websites and landing pages that make an immediate impression. No templates, no compromises.</p>
        <span class="service-arrow">↗</span>
      </div>
      <div class="service-card reveal reveal-delay-2">
        <span class="service-num">02</span>
        <h3 class="service-title">Web Development</h3>
        <p class="service-desc">Fast, accessible, production-grade code. Astro, React, or vanilla — we use the right tool for the job.</p>
        <span class="service-arrow">↗</span>
      </div>
      <div class="service-card reveal reveal-delay-3">
        <span class="service-num">03</span>
        <h3 class="service-title">Design Systems</h3>
        <p class="service-desc">Scalable, cohesive design systems that keep your brand consistent across every touchpoint.</p>
        <span class="service-arrow">↗</span>
      </div>
    </div>
  </div>
</section>
